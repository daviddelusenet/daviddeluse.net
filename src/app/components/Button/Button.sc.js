import styled from 'styled-components';
import { WHITE } from '../../utils/consts';

export default styled.button`
  background: none;
  line-height: 1;
  color: ${WHITE};
  font-size: 20px;
  border: 0;
  padding: 0;
`;
